language: go

os:
  - linux
  - osx

before_script:
  - gofmt -w .

go:
  - 1.7.x
  - 1.8.x
  - 1.9.x
  - tip

before_install:
  - go get github.com/mattn/goveralls

script:
  - goveralls -race -v -service=travis-ci
